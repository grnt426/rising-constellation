{
  "name": "rc-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Browser E2E tests for Tetrarchy Falls. Runs from the host against the worktree's Docker dev stack (reads ../.dev-ports.json for ports).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}

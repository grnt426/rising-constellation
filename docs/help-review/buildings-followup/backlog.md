# Backlog

## buildings

- The second sentence is awkward: "hold several, one on each body of its type" reads like a dangling fragment rather than a clear clause, and is a step down in clarity from the previous round's wording ("Every other body of its type in the system can have one too."), which read more smoothly.
- The sentence shifts from plural "your systems'" to singular "the system's" between its two clauses, a minor number mismatch right at the top of the page.
- The same fact is stated almost verbatim in two different sections of the page. Each placement is individually justifiable (context in one, list-completeness in the other), but a reader going through the page top to bottom hits it twice within a few paragraphs.
- nice_to_have: 'can never be destroyed' is said twice (in Infrastructure first and in Destroying a building). Not applied. Guide-map section 3, buildings OWNS item 3, puts 'can never be destroyed (next section)' in Infrastructure first, and item 6 lists 'Not an infrastructure building' under Destroying. The finding itself calls the repeat intentional. I only moved the sentence to lead its paragraph so the pronouns read cleanly.
- Voter stumble: 'The few buildings that need none say so on their page' does not name them. Not applied because the set depends on speed. The run (buildings-acc-r1-patents.exs) gives hab_open and university_open at Legacy and medium, but hab_open_poor and university_open at Flash. A hand-written list would be wrong at one speed, and no generated token lists it.
- Voter stumble: 'system visibility' is used without a link or a refresher. Human decision Q4: it stays plain text until the Erased batch writes the System Visibility page. Explaining the scale here would take over a topic this page does not own.
- Voter stumble: in the build-menu shot, mark (2) does not look disabled the way the padlocked mark (1) does. This is a screenshot capture issue (priv/help/shots/manifest.json, e2e/help-shots), outside this file. Rule 8 forbids describing the look in prose. The callback now names mark (2) explicitly.

## construction-queue

- "A tile takes one order at a time. While it is being built, upgraded or repaired, it takes no other order until that one finishes or is cancelled." restates the same rule twice in two consecutive sentences.
- In "Time estimates", the sentence "For a building that costs 1,200 production:" ends with a colon promising the worked example, but {shot:build-menu#cost} is inserted before the formula, so the screenshot lands between the colon and the thing it introduces.
- "current production" is phrased three different ways in three consecutive sentences: "at the system's current production", "at the current production", and "whenever the system's production changes".
- Developer (compiler rate/duration tokens): format per-hour values with thousands separators (2,000 / 240,000). No page edit is needed.

## upgrades

- The lead-in "For example:" before the indented infrastructure-cap example is a phrasing this leaf doesn't share with its sibling leaves.
- The Flash edge-case sentence sits between the infrastructure-cap example and the {table:upgrade_patents} table, so the paragraph flow jumps from a planet-cap example to Flash's lack of upgrades and back to a table about per-level patents.
- "The new level counts once the upgrade is finished" (intro) and "The infrastructure building's new level counts toward that cap only once its upgrade is finished" (cap bullet) restate the same timing rule almost verbatim.

## damaged-buildings

(no nice_to_have findings or deferred items recorded)

## siege

- The 'While besieged' bullet crams six blocked actions into one list item, which slows the reader down.
- The closing sentence of the Pillage yield section is harder to parse than it needs to be.

## production

- nice_to_have (units-legend sentence printed twice, once after each table): generated by the table generator (RC.Help.Tables / Format), so it cannot be fixed in this page's prose. Flag to the generator owner.
- Voter 1: 'Lex' and 'Tradition' appear unexplained in the Type column of the generated 'Other sources of production' table. It is generator output, and no Lex or tradition pages exist yet. Belongs to the generator owner or to the future lex and tradition catalog pages.
- Voter 1: 'Production per Local Population' (Industrial Spaceport row) introduces a second scaling stat. This is generated table text. A prose pointer would need the local-population alias that the link run adds to housing. Out of this brief.
- Voter 2: the arrow legend ('An effect with an arrow runs from the building's first level to its top level') prints below the table. It is generated by buildings_by_output, so it cannot be moved from the page.
- Out of scope for this file, found while verifying: priv/help/en/systems/star-systems.md:63 says 'Your capital starts with a higher base production' and :65 says 'it loses the higher base production'. At medium speed system_capital_base_production = system_base_production = 40 (run: tmp/help-review/production-reviser-consts.exs), so both sentences are false there. The star-systems owner should reword them to hold at every speed.
- {shot:production-tooltip#initial|The highlighted row, Initial value, is a capital's base production.} — the capture shows 100, which is the Legacy capital value; Medium and Fast pages print different numbers directly above the same image.
- star-systems.md:63: "Your capital starts with a higher base production. See [[production]]." — false at Medium speed (capital and other systems both start at 40).
- The reordered 'Base production' sentences leave the 'before bonuses' qualifier attached to only the first clause.
- The merged empty-queue bullet restates the same fact twice.
- 'Buildings' goes unlinked on its first mention on this page even though it has its own guide.

## administrative-operations

- The linking-run sentence reads "The system's construction queue stays too." instead of the brief's "Its construction queue stays too."
- "Buildings" and "population" in the first bullet are not linked, even though both now have their own manual pages.
- sources: lib/game/instance/stellar_system/stellar_system.ex:235-279 — the claim also rests on abandon/1 running to line 283 and on the tick draining the queue with no owner/status check (next_tick 899-905, resolve_production 1022-1024), neither of which is cited.

## housing

(light review — no findings recorded)

## stellar-bodies

- Minor word repetition around the new sentence: "first" appears twice in adjacent sentences and "infrastructure" three times across two sentences.
- guide-map.md §8 row for stellar-bodies should be updated to the wording now on the page: "A planet's other tiles need its [[infrastructure-building|infrastructure building]] first.", placed after the 'That is the {name:building.infra_open} ...' sentence. Out of this reviser's edit scope.
- Optional, not done because the brief is links only: add lib/game/instance/stellar_system/stellar_system.ex:378-380 to the page's sources, since the new sentence states that rule.

## workforce

(light review — no findings recorded)

## self-development

(light review — no findings recorded)

## colonization

(light review — no findings recorded)

## dominions

(light review — no findings recorded)

## star-systems

(light review — no findings recorded)

## system-outputs

(light review — no findings recorded)

## system-limits

(light review — no findings recorded)

## credit

(light review — no findings recorded)

## building/infra_open

- "no building there can rise above its level" — read literally, "no building there" includes the Megapolis itself, which does rise above its own level when upgraded. The brief says "no other building".

## building/infra_dome

- "no building there can rise above its level" — same as infra_open: includes the Central Hub itself.

## building/high_factory_dome

- Optional: 'the galaxy's first {name:building.high_factory_dome} finished in a player's system'. Or leave it as it is, since the case is rare (edge case: a system abandoned before the first finish means no announcement, and the 'first' stays open).
- Tutorial and Daily Challenge exclusion from the galaxy-first news — News.Server also ignores tutorial and daily instances (server.ex:495-517). Both are single-player, so 'every player' does not apply there. Only the Flash exclusion is stated, to keep the slot to one sentence under 25 words. A critic may want it added.

## building/monument_open

(no findings recorded)

## building/monument_dome

- Optional: 'the galaxy's first {name:building.monument_dome} finished in a player's system'. Or leave it as it is (same abandon-before-finish edge case as high_factory_dome).
- Tutorial and Daily Challenge exclusion from the galaxy-first news (same as high_factory_dome).

## building/finance_open

(no findings recorded)

## building/finance_orbital

(no findings recorded)

## building/shipyard_1_orbital

- "The ships it allows can still be ordered while it is being upgraded." — the prose slot renders before the card, Levels and Unlocking; the shell's "Ships built here" section (which names the class) is the last block on the page, so "The ships it allows" points at something the reader has not met yet.
- Running the 'ships already queued still finish after the shipyard is destroyed' claim — skipped: on a hand-built sim state, StellarSystem.remove_building crashes inside compute_bonus (partial state); the claim rests on code reading instead.

## building/shipyard_2_orbital

- "The ships it allows can still be ordered while it is being upgraded." — same forward-reference issue as shipyard_1_orbital.

## building/shipyard_3_orbital

- "The ships it allows can still be ordered while it is being upgraded." — same forward-reference issue as shipyard_1_orbital.

## building/shipyard_4_orbital

- "The ships it allows can still be ordered while it is being upgraded." — same forward-reference issue as shipyard_1_orbital.

## Consistency findings (deferred/backlog items owned outside a single page)

- construction-queue: put the shot before the rule sentence so the colon runs straight into the formula (build-menu screenshot vs. "For a building that costs 1,200 production:" example mismatch).
- administrative-operations / construction-queue: "So do the building, upgrade and repair orders in its construction queue." wording fix so the pointer sentence doesn't overclaim ship-order persistence.
- infra_open / infra_dome / upgrades: reword "Until it is finished, the planet's other tiles take no building, and no building there can rise above its level." so the level cap reads as permanent, not tied to "Until it is finished".
- dominions / credit / buildings: link ordering-related mentions to [[order-building]] instead of [[buildings]] now that the alias exists.
- production / construction-queue: production's "Buildings, repairs and ships all use it" list should include upgrades, to match construction-queue's order-type list.
- catalog.ex shipyard shell string / finance_open / finance_orbital / monument_dome / upgrades / buildings / workforce / housing: unify "stands built and not damaged" vs. "finished" terminology for a completed, undamaged building.
- upgrades / tables.ex (upgrade_patents) / catalog.ex (Levels intro): Flash-game "nothing can be upgraded" fact is stated three times across the compiled page (authored sentence, generated table note, generated catalog Levels intro).
- buildings: "The infrastructure building can never be destroyed." is stated in both "Infrastructure first" and "Destroying a building"; drop it from "Infrastructure first" and let "Destroying a building" own the rule.
- star-systems / buildings: the foreign-buildings alias (buildings#buildings-of-other-players) has no inbound link; add "For what you see of its buildings, see [[foreign-buildings]]." to star-systems, keeping "visibility" unlinked.

## Skipped edits

- production (linking run): (c) pointer sentence 'For the order of the queue, time estimates and cancelling, see [[construction-queue]].' — skipped because bullet 1 already links [[construction-queue|construction queue]] where the queue is first named, so the pointer would repeat the same link a few lines later. The task allowed skipping it only if (b) did not already cover it. Cancel and refund are search terms owned by construction-queue, so search still finds that page.
- all (linking run, lint result — not a skip, a record): Before edits the full live lint showed 0 errors and 4 warnings (map-legend dashes, stances semicolon, stances dashes, build-queue term clash). After edits it shows 0 errors and 3 warnings, all on map-legend and stances. Linting the 13 edited pages shows none of them. The admin-ops length warning introduced mid-run was cleared with length: long and a length_reason.
- building/shipyard_1_orbital (slot writer): Running the 'ships already queued still finish after the shipyard is destroyed' claim — skipped because on a hand-built sim state, StellarSystem.remove_building crashes inside compute_bonus (partial state); the claim rests on code reading instead (the same applies to the other three shipyard pages).
- building/high_factory_dome (slot writer): Tutorial and Daily Challenge exclusion from the galaxy-first news — skipped to keep the slot to one sentence under 25 words (the same applies to monument_dome).

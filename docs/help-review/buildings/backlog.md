# Backlog

## buildings

- The page's second sentence reads awkwardly for a player-facing tone (rule 1).: Split it, e.g. "Every building has its own page. Open it from the ? on its card."
- The `## Damage` section spells out the specific list of Navarch actions that damage buildings, duplicating wording that `damaged-buildings.md` owns almost verbatim, instead of using the guide brief's generic phrasing plus a link.: Use the guide brief's generic wording, e.g. "A Navarch's attack can damage buildings when it resolves. See [[siege]] for which ones.", and leave the specific action list to the linked pages.
- The callback "like the highlighted Limited building above" (in `## Unique and Limited buildings`) is a distant, imprecise reference to the `build-menu` shot shown two headings earlier.: Either repeat a small `{shot:build-menu#disabled}` here, or drop the backward reference and just state the rule: "The build menu does not let you order a building the limit blocks."
- Voter stumble: the ? on a building card appears in neither screenshot, so a reader cannot see where to look. HelpButton.vue:3 renders it only when the help manual is available, and the build-menu capture shows none. Beta flags are excluded scope and rule 8 forbids describing the button, so this needs a shot request: a building card with its ? help button marked, captured with the manual available.
- Voter stumble: the visibility table does not say where a player checks their visibility number for a system. How visibility is read and gained belongs to the future System Visibility page (Erased chapter). Per the human answer, 'system visibility' stays plain text here.
- Voter stumble: 'The infrastructure building also caps the level of every other building on its planet' has no example. The cap belongs to [[upgrades]], which this sentence already links. An example here would re-explain that page's rule.
- Voter note: box 2 in build-menu looks dimmed rather than clearly greyed. The prose no longer says 'greys out'. It says what the menu does and points at the highlight. Making the icon look more disabled is a UI or capture question, not page text.
- Possible accuracy edge in 'An agent of your faction in the system gives at least visibility 2': faction.ex:182 applies a diplomacy modifier after that minimum. Diplomacy is excluded scope, so I did not read it or check it, and the page keeps the sentence from the brief.
- Voter stumble, "patent" never defined or linked: no Patents page exists yet (grep for 'patent' in priv/help/en/systems and dominions finds nothing), and the brief says the patent tree is plain text until the Patents chapter. It becomes a link in a later linking run.
- Voter stumble, the Workforce column in {table:buildings_list} shows bare numbers with no unit: this is the generated D1 table and its legend, owned by the developer, not page prose. Suggest the table legend say what the number counts (workers mobilized) or link [[workforce]].
- Voter stumble, the Levels column reads 5 for every building at Legacy (and Tactic): real content, since Legacy and Tactic have 5 levels per building and Flash has 1 (guide-map section 2). It is a D1 generator choice (hide the column when it has one value at a speed, or explain it in the legend), not something to fix in prose.
- Voter stumble, "The infrastructure building also caps the level of every other building on its planet." felt dense: it is already its own paragraph with See [[upgrades]]. Spelling out the cap here would take over a rule that upgrades.md owns (guide-map section 7), so no change.

## construction-queue

- terms: [construction queue, build queue, queue, cancel, refund]: Remove 'build queue' from this page's terms for now. Or keep it here and have the later linking run take it off production.md, since this page owns the queue. production.md must not be edited in this run.
- The closing sentence adds an unbriefed fact about ship orders that isn't authorized by this leaf's brief and reads as inconsistent with the sentence right before it.: Either drop the sentence (it isn't in the brief) or add a one-clause transition that flags the exception explicitly, e.g. "...you can no longer cancel those orders. Ship orders are the exception: your Navarchs' ship orders there are cancelled instead, with no refund."
- The example number "1200" is not thousands-grouped, unlike the equivalent illustrative numbers on the sibling leaf damaged-buildings.md.: Write "1,200 production" in both the sentence and the indented example line to match damaged-buildings.md's convention.
- The two sentences opening the "one order per tile" idea restate the same point back to back.: Merge into one sentence, e.g. "A tile being built, upgraded or repaired takes no other order until that one finishes or is cancelled."
- Lint warning kept on purpose: "term `build queue` claimed by several pages: construction-queue, production". The brief requires the term on this page. production.md, which I may not edit, still lists it. Guide-map section 8 (linking plan, row `production`) removes it there in the later linking run, and the warning clears then. Everything else is clean: 0 errors, and no other warning for construction-queue (the summary's count of 4 includes other pages).
- The production-queue shot at the top still highlights the cancel button (mark 3) before the Cancelling section. Leaf shape (help-manual.md section 3.2) puts the screenshot right after the opening sentences, and the brief specifies production-queue#first,finish-time,cancel as one shot. The caption names the button, and the Cancelling section follows a few lines later. Showing the same image twice would add noise, so no change.
- Ship refund of credit and technology is left as written: 'A ship order gives back its credit and technology cost'. Ship order rules belong to the future Ships chapter, and no critic flagged the line.

## upgrades

- In a Flash game, every building has one level, so nothing can be upgraded. ... For example: {name:building.infra_open} at level 3: the other buildings on that planet can reach level 3 / {table:upgrade_patents}: Put a short lead-in right before {table:upgrade_patents}, for example 'The patent each upgrade level needs:', so the Flash version reads 'The patent each upgrade level needs: None'. You could also move the Flash sentence to the end of 'What each level needs', straight after the table. Then a Flash reader sees 'None' followed by the reason, and does not first read that nothing can be upgraded and then meet a level 3 example.
- In the "What each level needs" list, the sentence "The infrastructure building is [[building/infra_open]] on {name:patent_class.open} and [[building/infra_dome]] on {name:patent_class.dome}." is not part of the upgrades brief.: Cut the second sentence and let the existing [[infrastructure-building|infrastructure building]] link carry the reader to the buildings guide/catalog for which building that is, or fold the naming into the existing alias label instead of a separate stated sentence.
- The Flash edge case ("In a Flash game, every building has one level, so nothing can be upgraded.") sits between the opening bullet list and the "## What each level needs" heading.: Move the Flash sentence down to sit with the repair and siege edge cases, after the "What each level needs" section and table, matching the brief's item order (4, then 5, 6, 7).
- "The infrastructure building's new level counts toward that cap only once its upgrade is finished.": Restate the subject directly instead of relying on the backward reference, e.g. "The cap itself rises only once the infrastructure building's own upgrade finishes."
- Voter stumble: the example line 'Megapolis at level 3: the other buildings on that planet can reach level 3' renders as a <pre><code> block. Kept as is: the brief (guide-map section 3, upgrades item 4) asks for an example line, and rule 9 and the section 3.1 model page put example lines in an indented block. Changing how example blocks render is a compiler or CSS question for the developer, not a page edit.
- At Flash speed, {table:upgrade_patents} still renders 'None' under the level-requirements section (lib/rc/help/tables.ex, empty rows fall back to none(ctx)). The page cannot hide a section per speed because the token syntax has no speed-conditional text. The Flash sentence now comes first, which was the fix the critic asked for. A speed-conditional block or a Flash-specific table legend would need a developer change.

## damaged-buildings

- A building can be damaged when a Navarch's conquest, bombardment or pillage resolves. ... See [[siege]] for which buildings can be hit.: Add one plain-text clause after the first sentence, for example: "How many depends on the attack's result, and even a failed conquest or bombardment can damage some." Keep the Navarchs chapter as plain text and add no numbers, since attack results belong to Navarchs.
- The 'A damaged building:' bullet list breaks its own parallel grammar on the last item.: Rephrase the last bullet to keep the building as the implied subject, e.g. 'Blocks the system's ship orders, if it is a shipyard.' to match the voice of the other five bullets.
- The repair-cost example doesn't actually demonstrate the rounding it announces.: Swap one example figure for a number that doesn't divide evenly by the factor (e.g. 8,050 credits or 1,050 production) so the rounded result is visibly different from the raw product.

## Screenshot requests

- upgrades | A building card on the planet's tiles, previewing a level above the planet's infrastructure level, with the level pips and the 'Requirements unmet' state visible | marks: level-pips, requirements | why: It would show the infrastructure cap and the per-level requirement on the card itself, instead of explaining it only in words
- buildings | A system outside the player's faction, showing where its system visibility level is shown next to its bodies and tiles | marks: visibility | why: The visibility table uses levels 0 to 5, but readers cannot see where the game shows a system's current level
- damaged-buildings | The ship order menu of a system whose shipyard is damaged, with the affected ship class locked and its 'Building required' reason | marks: locked-ship | why: It would show the damaged-shipyard consequence the page states in one sentence

## Chart requests

(none)

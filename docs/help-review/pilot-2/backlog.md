# Backlog

## population

- {shot:system-population#growth,growth-bar|The growth word and the bar filling toward the next whole point, negative here because population is above housing.}: "The growth word and the bar that tracks the part of a point. Growth is negative here because population is above housing, so the bar is emptying."
- The Stability bullet under "What sets the speed" runs three full sentences before its link, where the guide map's format for this section calls for one or two.: Trim to two sentences, e.g. combine the cap and the below-0 case ("Stability speeds growth up to 25 stability, and slowly shrinks population below 0. See [[stability]]."), or move the below-0 detail to the stability leaf page.
- "Here is the exact growth of one tick, for players who want it:" types the unit "tick" as a bare word in prose instead of through a duration/rate token.: Rephrase without a bare unit word, e.g. "Here is the exact tick-by-tick formula, for players who want it:" or drop the time reference entirely since the block is self-evidently the per-tick code.
- The defense bullet states a rate ("each whole point adds {const:system_base_defense} defense") without a worked example, unlike the fuller treatment the design doc's own template gives a comparable per-point rate.: Add a short concrete instantiation, e.g. "20 workforce adds 3 defense," or leave as is if the guide map's brief (which only asks for "defense in a system you own") is meant to keep this terse.
- Voter stumble: the screenshot's '2/15', '7.5' and '22' readouts are not explained. The brief assigns this page only the growth and growth-bar marks. The workforce, housing and stability readouts belong to the workforce, housing and stability leaves.
- Voter stumble: stability's link to growth still appears in three places: the intro sentence, the Stability bullet, and the 'costs stability' item in the 'What population gives' list. The brief requires both the bullet and the list item, so I only added a [[stability]] pointer to the intro instead of removing it.

## housing

- Example with 16 workforce: planet with 13 housing from buildings: 13 population / planet with 3 housing from buildings: 3 population: Pick a workforce different from the total housing so the split is clearly proportional, e.g. 8 workforce with the same planets gives [7, 1]. Run apportion_workforce to confirm before writing it. Or add one line: the shares always add up to the system's workforce, not to its housing.
- "Each planet gets a share in proportion to the housing its own buildings give." reads awkwardly for a phrase meant to be a simple, friendly sentence (style rule 1).: Reword to something like "Each planet's share matches how much housing its own buildings give."
- "A planet's share is recalculated only when the system's workforce changes by a whole point." is stated with no consequence attached.: Either drop the sentence (it is not called out in the guide map's owns list for housing) or add the one consequence a player would notice, e.g. "so a planet's number can lag a tick behind a workforce change."
- "Gas giants and asteroid belts have no tiles, so they never get a share." and "Moons and asteroids never get a share either. No building there gives housing." are three short sentences all making the same "gets no share" point about different body types.: Combine into one bullet, e.g. "Gas giants, asteroid belts, moons and asteroids never get a share. None of them can build housing."

## stability

- Even a failure costs 5.: Change to: "Even a failure costs 5. A critical failure costs nothing." The literals are already covered by the encourage_hate.ex:59-80 source.
- It speeds up population growth. See [[population]].: If it fits under the leaf word cap, add one list item: "Below 0, population shrinks. See [[population]]." Or confirm the population guide states it.
- sources: frontmatter: Add lib/data/game/content/constant-slow.ex:9-18 and lib/data/game/content/population_status.ex:11-17 to sources.
- The screenshot caption reads as two separate sentences rather than one.: Tighten to a single sentence covering both marks, e.g. "1. From buildings. 2. Lost to population." or fold both numbers into one clause.
- The opening sentence's word choice is slightly stiff for the plain, friendly voice rule 1 asks for.: Consider "Stability is how happy a system's population is," which is simpler and avoids the noun/adjective ambiguity.
- "Costs" in the Temporary penalties section could be misread as a price paid by the Siderian rather than stability lost by the target system.: e.g. "A success removes 15 stability from the target and a critical success removes 20." to keep the subject unambiguous.
- Voter stumbles about words the tables use without explaining them ('Appeal Potential', 'Lex' names like Second Core, the 'level 1 → max' arrow ranges) are not fixed. Those come from the generated tables and there is no page to link to. Explaining the arrow ranges belongs in the table renderer or a shared legend, not in prose on this leaf.
- Siderian and Destabilize are not linked because the linkable-page list has no agents or actions page. This can be fixed once such a page exists.
- nice_to_have 2 (shot request): capture a stability tooltip on a system that has one or more active Destabilize penalties, showing each penalty's own line with the amount left. That shot could replace the sentence "Each penalty has its own line in the stability tooltip, showing what is left." The page frontmatter has no shot_requests field, so the request is recorded here.
- Voter stumble about the arrow ranges in the buildings table ("+1.6 → +10") and the mixed "per Appeal Potential" / "per Local Population" bases: that text comes from the {table:buildings_by_output} generator, not from this page's prose, so any fix belongs in the table generator or a shared legend.
- Voter stumble that mark 1 covers the whole Buildings block: this comes from the mark geometry in priv/help/shots/manifest.json, which this reviser may not edit.

## workforce

- Buildings mobilize part of it.: Say "Buildings mobilize workforce." (or "Buildings use it.") without "part of".
- Each building mobilizes a fixed workforce, the same at every level.: Add a few words, for example: "Each building mobilizes a fixed workforce, the same at every level. Some buildings mobilize none."
- Caption: "Mobilized workforce and total workforce." (system-population#workforce): Use UI tokens in the caption, for example "{ui:galaxy.system.population.workforce_mobilized} and {ui:galaxy.system.population.workforce_total} workforce." Or add one sentence saying the game labels this readout Population.
- List 'Which buildings mobilize:' includes 'A building under construction mobilizes nothing until it is finished.' and 'Demolishing a building frees its workforce at once.': Keep the three items that mobilize in the list, and move the construction and demolition statements to one or two sentences after it.
- "Each building mobilizes a fixed workforce, the same at every level." says the same thing twice ("fixed" and "the same at every level" are redundant restatements of one fact).: Tighten to one clause, e.g. "Each building mobilizes a fixed workforce, at every level." or "A building's workforce cost never changes with its level."
- The bullet "A damaged building, even though it gives no bonuses." breaks the parallel pattern of the other four bullets (which each just name a building state that still mobilizes workforce) by adding an aside about bonuses.: Either drop the "even though it gives no bonuses" clause here (it's system-penalties/building territory) or move it to its own one-sentence edge-case note per the leaf shape's step 5.
- The lone "## Over-mobilization" heading sits over a single paragraph, one indented example, and one closing sentence.: Consider dropping the heading and letting the over-mobilization paragraph follow directly after the bullet list, since the page has no other sections to distinguish it from.

## population-class

- Each class is worth a number of {ui:panel.help.legend_mode_population_name} ("System points").: Optionally name the victory panel label in the victory sentence as well (via a data-key token if the compiler supports it, else plain text quoting it). "star system points" is already in terms.
- The system view shows the points next to the owner's name.: Optionally extend the one sentence: hovering the points shows the system's class and the full class list.
- The victory-path sentence reads redundantly once the {name:} token expands.: Drop the trailing "victory path", e.g. "That total is its score on the {name:victory.population}.", or rephrase as "...its {name:victory.population} score."
- The rounding bullet has two sentences that partly restate the same fact.: Merge into one sentence, e.g. "The class uses the exact population, not the rounded one, so a system just short of a threshold stays in the class below." (or drop the second sentence).
- "Each class is worth a number of {ui:...} points" reads slightly stilted.: Consider "Each class is worth System points" or "A higher class is worth more System points."
- MUST_FIX, needs someone who can edit the generator (outside this reviser's page-only scope): the {table:population_classes} third column header is wrong. It comes from lib/rc/help/format.ex:31 `victory_points: "Victory points"` (fr: format.ex:76 "Points de victoire") via tables.ex:46. The finding is correct: victory.ex:345 feeds class points into population_track.points, while real victory points are milestone values from threshold_values [0, 2, 5, 10] (victory.ex:386-393). Fix: add a new format key such as `system_points: "Star System Points"` (or "System points" to match game.json legend_mode_population_name), give it a French translation, and use it at tables.ex:46. Also fix the tables.ex:14 doc row ("victory points"). Until then the rendered page still shows a "Victory points" header above prose that says System points.
- Not added: a note that the points show "?" when you lack intel on the system (visibility 3, faction/stellar_system.ex:66), or that neutral systems show them too. The brief allows one sentence at most on where the class shows, and that sentence now covers the hover popover.
- must_fix 1 (table header "Victory points"): the finding is correct, but I could not fix it within my edit scope. It needs a generator change I am not allowed to make (only the page .md is in scope). Fix: in lib/rc/help/format.ex:31 (en) and :76 (fr), rename victory_points: "Victory points" / "Points de victoire" to the UI's name for these points (en "System points", from game.json panel.help.legend_mode_population_name), or point tables.ex:46 at that UI string. The rendered --text still shows "Victory points" in the header until then. The page prose now uses only "System points", so the header is the only mismatch left.
- Nice to have (a token for data.json victory.population.points = "Star System Points"): no token can reach a non-name leaf key. I sidestepped it by dropping the literal from the page instead of asking for a new token.
- shot_request: a capture of the system-view owner marker label (the population class points) and its hover popover (class title, points, list of all classes). That would let the one-sentence UI description become a {shot:}.

## population-status

- The system's state tab shows the current status.: Keep it to one sentence without the tab name. For example: "An unstable system shows its status at the top of its bodies list." Or drop the sentence and add a shot_request for the status bar (the design doc already says an unstable-system scene is needed).
- You get a notification when population, a finished or removed building, or a Destabilize pushes your system out of Normal.: Optional. The brief allows one sentence, so either keep it as it is or simplify to "You get a notification when a building, population change or Destabilize pushes your system out of Normal." Do not add a list of silent cases unless it fits in one short sentence.
- frontmatter sources: lib/game/instance/stellar_system/stellar_system.ex:275-330: Replace 275-330 with stellar_system.ex:567-573, 867-872, 1186-1196, 1204-1222, and add front/src/game/components/galaxy/system/Bodies.vue:3-6.
- The notification sentence lists three trigger causes with inconsistent noun phrasing: "You get a notification when population, a finished or removed building, or a Destabilize pushes your system out of Normal.": Make the three items parallel events, for example "a change in population, a finished or removed building, or a Destabilize".
- Frontmatter lists terms: [population status, population productivity], but "population productivity" never appears in, or is explained by, the page's prose.: Either drop "population productivity" from terms: if it belongs elsewhere, or add a one-clause tie-in (e.g. note that the penalty is what UI calls productivity loss) so the term is actually covered.

## system-penalties

- production 150, 12 workforce, 15 mobilized: 150 − 20 % = 120 / also Protests (25 %): 120 − 25 % = 90 / also besieged: production 0: Put the example in the code and tooltip order. For example: over-mobilized plus Protests first (150 → 120 → 90). Then a separate line: besieged, production is 0 before the other two apply. Or just say the besieged line comes first in the tooltip.
- also {name:population_status.demonstration} (25 %): Add lib/data/game/content/population_status.ex to sources (the demonstration entry). Or reword so the rate comes from the population-status table, for example 'a status with a 25 % penalty' with the source cited.
- They never reduce [[stability]], [[housing]], [[mobility]] or S.L.S.D.: Use {name:bonus_pipeline_out.sys_radar} for S.L.S.D. You could also note in a few words that credits from the mobility bonus count as Credit and are reduced, or leave that to the mobility page.
- The bundled bullet item mixes four separate outputs into one comma-joined line while every other item in the same list is a single output.: Split the four ship-XP outputs into their own bullets (or into a short sub-list) so every line in "What they reduce" is a single item, matching the rest of the list.
- The Insufficient stability table row gives no size information, unlike its two sibling rows.: Give the third row a concrete size phrase (for example "the status's fixed percentage, see [[population-status]]") so all three Size cells answer the same question at a glance.
- The worked example silently switches from an invented scenario to a real status name and its real penalty value without signposting the change.: Add a short lead-in word such as "say" or "if it's also" before the Protests line so the reader can tell the whole block is one continuous illustrative example rather than a mix of invented and literal figures.

## credit

- Mobility adds more credits on top of taxes. The bonus grows with the system's workforce. See [[mobility]].: Put the mobility value back in, for example: "The bonus grows with the system's workforce and its mobility." It stays two sentences.
- Voter stumble: 'Lex' in the Other sources table has no explanation. There is no Lex page in the linkable list, and saying what a Lex is falls outside this guide's brief. It needs a Lex catalog or glossary link later.
- Voter stumble: 'Erased — Finance' agent skill has no context. This is generated table content (the em dash comes from the table generator, not the prose). An agent-skill page is out of this category.
- Voter stumble: the mobility section gives no numbers. The brief keeps the mobility example and formula on [[mobility]] only (one or two sentences here).
- Voter stumble: the highlighted Taxes row sits under a MISCELLANEOUS heading in the screenshot. I left the caption as is. The highlight box already shows the row, and rule 8 allows only one caption sentence.
- Voter stumble: 'Centralization of Power' (Lex) and 'Erased — Finance' (Agent skill) are not explained. These rows come from the generated {table:bonus_sources sys_credit}, and rule 10 says prose never restates a table row. No Lex or agent page is on the linkable list, so I left the table as is.
- Voter stumble: whether '+2 Credit per Population' from Centralization of Power works like taxes is unclear. The wording comes from the table generator, not from page prose, so a fix belongs in the table effect wording.
- Lexes that act on player_credit directly (for example doctrine-medium.ex:347, +100) are not in the income sentence. The reviewer called this a smaller gap, and adding it would make the one sentence longer. It can be added as 'and your Lexes' if a later round wants it.

## mobility

- Each point of mobility adds {rate:system_mobility_taxes_factor|credits} for every point of [[workforce]]. (And the 'Other sources of mobility' table lists Trade Secrets +10 % and Aeronautical Tycoon +15 % Mobility.): Needs a human decision first. Is the ordering intended, or is it a defect? (Per the gameplay-expectation rule, ask before documenting either.) If it is live behavior to keep, add one sentence to the page, for example: 'Percentage mobility bonuses count toward the Mobility bonus only when the system also has a building that turns mobility into credits.' If it is a defect, leave the page as written and file the bug.
- The abstract rate sentence largely duplicates the worked example that follows it.: Drop the abstract rate sentence and fold its content into the example sentence, e.g. "Each point of mobility adds {rate:system_mobility_taxes_factor|credits} for every point of [[workforce]]: a system with 20 workforce and 8 mobility gets 20 x 8 x {const:system_mobility_taxes_factor} = {rate:16|credits}." (or otherwise merge into one line) so the formula appears once.
- The variable order in the prose sentence does not match the order in the numeric example.: Use the same order (workforce, then mobility) in both places, matching the guide map's stated formula order "workforce x mobility x factor".

## game-time

- "A daily runs on its own faster clock, which is not in this table." (Game speed) and later "In a daily, the manual shows Legacy numbers, and its hours are Legacy hours." (Ticks and hours): Add a short clause tying them together where the second fact appears, e.g. "A daily's own clock is faster, but the manual still shows Legacy numbers and Legacy hours for it."
- "The months run from α-Tetran to ζ-Quadrinople.": If kept as one sentence (per the calendar's flavor framing), consider naming the pattern explicitly, e.g. "the months run α to ζ across four root names, from α-Tetran to ζ-Quadrinople," or drop the range if it doesn't earn its place.
- The page uses no {icon:} tokens anywhere (e.g. before "credits" in the taxes example, or for population): Optionally add {icon:resource/population} and {icon:resource/credit} to the taxes example sentence for visual consistency with other pages.
- Surface defect for the orchestrator (belongs in the compiler or store, not this page): per-hour help values in a daily are off by 240x. The help modal and Manual tab show help-unit-hour whenever incomePerHour is true, at every speed (HelpOverlay.vue:134, Manual.vue:132). A daily loads the Legacy bundle compiled at 20 ticks per hour (store.js:30, help_controller.ex:13), but the daily clock is factor 240, which is 4800 ticks per hour. The game's own income filter only converts on Legacy (game/store.js:531 sets ticks per hour to 20 only for 'slow'). The Settings label also says 'Per hour (Legacy games)', while the help manual converts at Tactic and Flash too. The fix is to convert with the daily factor or hide the hour variant in dailies, and to settle the Tactic/Flash mismatch with the setting label.
- Reviewer stumble (the '40 credits per hour' example does not say which speed it uses): skipped. In game, the page compiles per instance speed, so a typed 'Legacy' label would be wrong at Tactic or Flash. Showing the speed next to numbers (rule 13) is a surface or compiler concern.
- nice_to_have (the top-bar calendar is described in words): kept as one sentence because no shot exists. Shot request: a top-bar calendar/date capture (navbar/Calendar.vue) so 'The date in the top bar' can become a {shot:}.

## Screenshot requests

- population-status | PopulationStatus.vue status bar on the system State tab, for a system whose stability is below 0 (for example Protests) |  | The status ladder and its productivity are UI the page can only mention in one sentence
- population-class | Properties.vue System points number next to the owner name, with its class tooltip open |  | Shows where the class and its points appear
- stability | Stability tooltip of a system hit by a Destabilize, showing the Temporary penalties heading and a Destabilization line |  | The temporary penalty tooltip line is described in words today
- system-penalties | Production or credit tooltip of a system that is over-mobilized and in Protests, showing Over-mobilized population and Insufficient stability lines |  | Would show the penalty labels and how they reduce an output
- workforce | system-population box with mobilized above total (for example 15/12), with the readout highlighted |  | Shows what over-mobilization looks like in the Population box
- housing | Bodies list of a system with two inhabited planets, each with its own population badge |  | The current shot shows only one inhabited planet, so the sharing is not visible
- mobility | credit-tooltip with a Mobility bonus row: the system's Credit breakdown popover for a system that has mobility (for example a sterile planet with an Industrial Spaceport), showing both the Taxes and Mobility bonus rows |  | The current capture has no Mobility bonus row, so neither the mobility leaf nor the credit guide's Mobility bonus section can point at the line the bonus shows up as. A new scene with mobility above 0 is needed.
- mobility | system view Details box with the Mobility readout (front/src/game/components/galaxy/system/Details.vue) |  | Leaf shape step 2: it would show where a system's mobility value appears, without describing that place in words.
- game-time | navbar-calendar: the in-game date box in the top bar (front/src/game/components/navbar/Calendar.vue, .navbar-calendar), showing day, month name such as α-Tetran, and year |  | The calendar section says in words that the date sits in the top bar. A shot would replace that sentence.
- game-time | portal settings Income display option (front/src/portal/pages/Settings.vue:55-80), both radio choices |  | The page names the account setting the in-game manual follows. Its choices cannot be quoted in prose without tripping the typed-time lint, so a shot would show them.

## Chart requests

- population_shrink housing=<n> start=<n> bonus=<n,...>: population that starts above housing + 0.75 (for example after a housing building is damaged) shrinking back toward it, one line per stability, to show that higher stability shrinks it faster (for the population guide or housing)
